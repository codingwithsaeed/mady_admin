import 'package:flutter/material.dart';

class SellersPage extends StatelessWidget {
  const SellersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SellersPage'),
      ),
    );
  }
}